This project was very challenging as I had to implement a basic version of all the functions and logic to get the code to compile. I also had to learn a lot about Binary Search Trees implementations. There was a lot of things I did not understand at first and just kept having to tweak to get to work like figuring out what each method is supposed to do for the BST implementation. If I had more instructions it would've been a lot more helpful, I felt pretty lost but it did also feel like I was working in a real codebase because of the lack of documentation. I realized very late that the ID is not supposed to be uniquely random but like in an actual database, an autoincreminting integer. I also had a hard time with the prefix function because figuring out I had to add the last ascii code. 
I did learn a lot about how Binary Trees work in practice rather than just theory. As well as understanding how database engines work deep down. Lambda functions also make more sense now and they seem pretty interesting but I do not know if I can make a lambda function easily. 